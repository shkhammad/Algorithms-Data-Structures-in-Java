If the size of the list will not change overtime(constant), it is better to use arrays since 
they are typically faster than arraylist, else if the size would change overtime
then use dynamic arrays(arraylist). 